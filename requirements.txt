# ── Web server (portable runtime — Phase 1) ──────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# ── Core GUI and Web Engine (desktop mode) ───────────────────────────────
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# Markdown Processing
markdown2>=2.4.0

# Document Export
python-docx>=1.1.0

# AI Integration (optional)
httpx>=0.25.0

# Optional: PDF export via weasyprint (requires GTK on Windows)
# weasyprint>=60.0

# Build tool (for creating exe)
# pyinstaller>=6.0.0
pdf2docx>=0.5.6
openpyxl>=3.1
python-pptx>=0.6.23
pymupdf>=1.22.5
pymupdf>=1.22.5
python-dateutil>=2.8.2
